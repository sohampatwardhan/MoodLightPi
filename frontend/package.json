{
  "name": "moodlightpi-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Preact + TypeScript SPA for MoodLightPi, built by Vite into ../web-dist for rust-embed.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "preact": "^10.24.3"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.1",
    "typescript": "^5.6.3",
    "vite": "^7.0.0",
    "vitest": "^3.0.0"
  }
}
